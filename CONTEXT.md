# 庆事通公开领域上下文

## 定位

庆事通是面向校园服务场景的独立 AI 应用原型。项目可以以广州大学校园问题为比赛研究背景，但不代表广州大学官方项目、学校背书、当前校务规则或真实系统接入。

公开仓库只包含抽象领域模型、重写后的示例和虚拟数据。真实公开资料、授权可见资料、登录后页面和任何个人记录都属于本地研究资料边界，不进入公开仓库。

## 稳定边界

- 学生看到的是事项、依据、材料、下一步和模拟状态；不是工程 Trace、检索分数或 Provider 配置。
- RAG 只能消费经过登记、脱敏/重写、审核并发布的知识版本。
- Agent 只能使用服务端声明的只读工具；确定性 Workflow 负责字段、规则、状态和模拟副作用。
- 预览与确认分离。未确认不能推进模拟状态，重复确认不能重复写入。
- 没有证据、存在冲突或来源过期时，系统应澄清、降级、拒答或引导核验。
- 模拟渠道不拥有融合门户、企业微信或其他平台的身份、权限和回执。
- 庆小通是表现层伴随形象，不拥有独立权限、长期业务记忆或第二套状态机。

## 术语

**虚拟业务数据**：为复现业务结构而设计的示例内容。它可以借鉴通用校园流程形状，但不等于任何学校当前事实。

**Publication**：一组经过版本、来源、质量和责任检查后可供检索消费的知识快照。

**Evidence Gate**：回答生成前后检查结论、条件和材料是否能够由已发布 Evidence 支持的规则层。

**模拟办理任务**：只写入本地演示数据库的可恢复任务，不代表真实办件、审批或回执。

**真实接入**：获得明确身份、权限、接口范围、责任人和审计约束后的外部系统连接。当前项目没有此项。

## 状态语言

文档必须区分：设计目标、代码存在、测试通过、当前本地验证、授权后规划和真实系统接入。测试或截图不能替代授权，设计文档不能替代运行证据。
